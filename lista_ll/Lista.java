class Lista{
    private No first, last;
    private int tam;
    
    public Lista(){
        first = null;
        last = null;
        tam = 0;
    }

    /**
     * MÉTODOS PARA ACESSAR
     * - before(n)
     * - after(n)
     * MÉTODOS PARA ATUALIZAR
     * - replaceElement(n, o)
     * - swapElements(n, q)
     * - insertBefore(n, o)
     * - insertAfter(n, o)
     * - remove(n)
     */

     

}